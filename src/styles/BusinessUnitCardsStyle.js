import styled from 'styled-components';

const BusinessUnitCardsStyle = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(500px, 1fr));
  gap: 40px;
`;

export default BusinessUnitCardsStyle;
