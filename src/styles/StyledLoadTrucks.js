import styled from 'styled-components';
import CreateOrderStyle from './CreateOrderStyle';

const StyledLoadTrucks = styled(CreateOrderStyle)`
  background-color: transparent;
`;

export default StyledLoadTrucks;
